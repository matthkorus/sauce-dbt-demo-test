selectors:
  - name: select_root_models
    description: This selector will build any models in the root models directory. 
    definition: 
      method: path
      value: "models/*.sql"
  - name: refresh_model_test
    description: for testing a single model
    definition: 
      method: path
      value: "models/refresh_test_model.sql"
